- hosts: webservers
  vars_prompt:
    - name: "vaultRoleId"
      prompt: "Vault Role ID for Ansible"
    - name: "vaultSecretID"
      prompt: "Vault Secret for Ansible"
  roles:
  - role: nginx
  - role: dashboard
  become: yes
