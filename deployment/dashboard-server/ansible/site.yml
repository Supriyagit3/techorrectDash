- hosts: webservers
  remote_user: ec2-user
  vars_prompt:
    - name: "vaultRoleId"
      prompt: "Vault Role ID for Ansible"
    - name: "vaultSecretId"
      prompt: "Vault Secret for Ansible"
  roles:
  - role: nginx
  - role: dashboard
